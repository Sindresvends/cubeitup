trigger:
- master

pool:
  vmImage: windows-latest

variables:
- name: deploymentFile 
  value: ArmTemplates/AppServicedeploy.yml

- template: ${{ variables.deploymentFile }}
  parameters: 
    environmentPrefix: d

# - template: $deploymentFile
#   parameters: p